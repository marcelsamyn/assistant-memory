import { describe, expect, it } from "vitest";

describe("metric source helpers", () => {
  it("keeps metric source helper coverage in database-backed tests", () => {
    expect(true).toBe(true);
  });
});
